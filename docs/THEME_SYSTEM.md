# THEME_SYSTEM — 테마 10종

> 정책은 [`../CLAUDE.md`](../CLAUDE.md) §9. 2026-08-14 기획 확정 — 구현 0%.
> **전부 무료.** 유료 테마·커스텀 테마는 MVP 밖(커스텀은 P2).

## 구현 현황

| 영역 | 상태 |
|---|---|
| 테마 토큰 구조(`theme/palettes.ts`) | ❌ |
| 10종 팔레트 정의 | ❌ |
| 선택 UX(Settings → Theme, 미리보기 그리드) | ❌ |
| 선택 로컬 저장 | ❌ |

---

## 1. 테마 목록 (기획 확정 · 시안 확정 2026-08-14)

**화면 시안 정본: [`design/theme-mockups-10.png`](./design/theme-mockups-10.png)** (2026-08-14 사용자 제공).
구현 시 팔레트 값은 이 시안에서 추출한다. 시안이 보여주는 홈 구조(검색바 → 즐겨찾기(+편집) →
전체 사이트(계정 수 배지) → 하단 네비, 가운데 `+`는 원형 강조 버튼)도 함께 정본이다.
⚠ 단 **네비 탭 구성은 예외** — 시안은 5탭이지만 ~~4탭~~ → **3탭(Home·Favorites·Settings)이 정본**이고
검색·추가는 홈 우상단 헤더 버튼이다(2026-08-14 사용자 재정정, SITE_SYSTEM §7). 시안은 팔레트·톤 기준으로 쓴다.

| # | 이름 | 시안 라벨 | 방향(시안 기준) |
|---|---|---|---|
| 01 | Minimal Light | 미니멀 라이트 | 화이트 배경 + 블루 포인트. 깔끔하고 가벼운 기본 테마 (기본값 후보) |
| 02 | Dark Modern | 다크 모던 | 차콜/블랙 배경 + 블루 포인트 모던 다크 |
| 03 | Nature Green | 네이처 그린 | 화이트 배경 + 그린 포인트·연그린 카드 |
| 04 | Sky Blue | 스카이 블루 | 밝은 하늘색 그라데이션 배경 + 블루 포인트 |
| 05 | Warm Beige | 웜 베이지 | 크림/베이지 배경 + 브라운 포인트 |
| 06 | Neon Cyber | 네온 사이버 | 딥 블랙 배경 + 네온 마젠타/핑크 포인트·네온 보더 |
| 07 | Carbon Gray | 카본 그레이 | 다크 그레이 무채색, 포인트도 그레이 — 차분·전문적 |
| 08 | Pastel Lavender | 파스텔 라벤더 | 연보라 배경 + 퍼플 포인트 |
| 09 | Yellow Point | 옐로우 포인트 | 화이트 배경 + 옐로우/앰버 포인트 |
| 10 | Ocean Deep | 오션 딥 | 딥 네이비 배경 + 블루 포인트 |

---

## 2. 적용 범위 — 테마는 디자인 세트다

배경색만 바꾸는 게 아니다. 다음 토큰을 **하나의 세트**로 관리하고, 테마 변경 시 앱 전체가 함께 바뀐다:

`Background · Surface · Card · Search Bar · Primary · Secondary · Text · Icon · Button ·
Navigation · Selected State · Favorite Area · Badge`

- 구현은 조각의 `theme/palettes.ts` 방식 승계 — **팔레트 객체 하나 추가 = 테마 하나 추가**가 되도록
  토큰 인터페이스를 고정한다. 컴포넌트는 토큰만 참조하고 색상 리터럴을 직접 쓰지 않는다.
- 광고 배너 영역·시스템 상태바 스타일(light/dark content)도 테마 토큰에서 파생한다 —
  다크 계열 테마(02·06·10)에서 상태바가 검은 글자로 남으면 안 된다.

---

## 3. 선택 UX

```
Settings → Theme

Current: Minimal Light

┌──────────┐ ┌──────────┐
│ Preview  │ │ Preview  │      ← 이름 + 실제 UI 축소 미리보기
│ Minimal  │ │ Dark     │        (홈 화면 미니어처: 검색바·카드·네비)
│ Light    │ │ Modern   │
└──────────┘ └──────────┘
```

- 미리보기는 스크린샷 이미지가 아니라 **토큰으로 그린 미니어처 컴포넌트**로 — 팔레트를 추가하면
  미리보기도 공짜로 따라온다(이미지 10장 유지보수 회피).
- 선택 즉시 적용 + 로컬 저장(재시작 후 유지).

---

## 4. 미결정

- **기본 테마** — Minimal Light 유력. 시스템 다크모드일 때 Dark Modern으로 자동 선택할지,
  시스템 설정과 무관하게 사용자 선택만 따를지. (10종 테마와 라이트/다크 자동 전환은 축이 다르다 —
  섞으면 "다크모드인데 Warm Beige면?"이 생긴다. 구현 전 매듭.)
- 테마별 배너 광고 배경 처리(광고는 테마를 모른다 — 경계선/여백으로 흡수).
